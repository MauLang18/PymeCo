@model IEnumerable<POS.Domain.Entities.Client>

@{
    ViewBag.Title = "Listado de Clientes";
}

<div class="w-full px-6 py-8">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
        <h1 class="text-2xl font-bold text-[color:var(--txt-primary)]">@ViewBag.Title</h1>

        <div class="flex items-center gap-3 mt-4 sm:mt-0">
            @if (User.IsInRole("Admin") || User.IsInRole("Vendedor"))
            {
                <a asp-controller="Client" asp-action="CreateClient"
                   class="px-4 py-2 rounded-xl text-white font-medium
                          bg-[color:var(--primary)] hover:bg-[color:var(--primary-dark)] transition-colors">
                    <i data-lucide="plus" class="inline w-4 h-4 mr-1"></i>
                    Nuevo Cliente
                </a>
            }

            <a href="@Url.Action("ExportExcel", "Client", new { q = Context.Request.Query["q"].ToString() })"
               class="px-4 py-2 rounded-xl border border-emerald-600 text-emerald-700
                      hover:bg-emerald-600 hover:text-white transition-colors font-medium">
                <i data-lucide="file-spreadsheet" class="inline w-4 h-4 mr-1"></i>
                Exportar Excel
            </a>

            <button id="refreshClients"
                    type="button"
                    class="px-4 py-2 rounded-xl border border-[color:var(--primary)]
                           text-[color:var(--primary)]
                           hover:bg-[color:var(--primary)] hover:text-white
                           transition-colors font-medium">
                <i data-lucide="rotate-cw" class="inline w-4 h-4 mr-1"></i>
                Refrescar
            </button>
        </div>
    </div>

    <div class="w-full overflow-x-auto shadow-sm rounded-2xl border border-[color:var(--border)]
                bg-[color:var(--light-panel)]">
        <table class="min-w-full text-sm text-left text-[color:var(--txt-secondary)]">
            <thead class="bg-[color:var(--light-bg)] text-[color:var(--txt-primary)] text-xs uppercase">
                <tr>
                    <th class="px-6 py-3 text-center font-semibold">Acciones</th>
                    <th class="px-6 py-3 font-semibold">Nombre</th>
                    <th class="px-6 py-3 font-semibold">Cédula</th>
                    <th class="px-6 py-3 font-semibold">Correo</th>
                    <th class="px-6 py-3 font-semibold">Teléfono</th>
                    <th class="px-6 py-3 font-semibold">Dirección</th>
                </tr>
            </thead>

            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var c in Model)
                    {
                        <tr class="border-t border-[color:var(--border)] hover:bg-[color:var(--light-bg)]">
                            <td class="px-6 py-4 text-center">
                                <div class="flex items-center justify-center gap-4">
                                    @if (User.IsInRole("Admin") || User.IsInRole("Vendedor"))
                                    {
                                        <a asp-action="EditClient" asp-route-id="@c.Id">
                                            <i data-lucide="pencil-line" class="w-5 h-5"></i>
                                        </a>
                                    }

                                    @if (User.IsInRole("Admin"))
                                    {
                                        <a asp-action="DeleteClient" asp-route-id="@c.Id">
                                            <i data-lucide="trash-2" class="w-5 h-5"></i>
                                        </a>
                                    }

                                    <a asp-action="DetailsClient" asp-route-id="@c.Id">
                                        <i data-lucide="eye" class="w-5 h-5"></i>
                                    </a>
                                </div>
                            </td>

                            <td class="px-6 py-4">@c.Name</td>
                            <td class="px-6 py-4">@(!string.IsNullOrWhiteSpace(c.NationalId) ? c.NationalId : "—")</td>
                            <td class="px-6 py-4">@(!string.IsNullOrWhiteSpace(c.Email) ? c.Email : "—")</td>
                            <td class="px-6 py-4">@(!string.IsNullOrWhiteSpace(c.Phone) ? c.Phone : "—")</td>
                            <td class="px-6 py-4">@(!string.IsNullOrWhiteSpace(c.Address) ? c.Address : "—")</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" class="px-6 py-6 text-center text-gray-400">
                            No hay clientes registrados.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            const btn = document.getElementById('refreshClients');
            if (!btn) return;

            btn.addEventListener('click', () => {
                const qs = window.location.search || '';
                const base = '@Url.Action("ListClient","Client")';
                const sep = qs ? '&' : '?';
                const url = `${base}${qs}${sep}_ts=${Date.now()}`;

                btn.disabled = true;
                btn.classList.add('opacity-70', 'cursor-wait');
                btn.innerHTML =
                    '<i data-lucide="loader" class="inline w-4 h-4 mr-1 animate-spin"></i> Actualizando...';

                window.location.assign(url);
            });

            if (window.lucide?.createIcons) {
                window.lucide.createIcons();
            }
        })();
    </script>
}

