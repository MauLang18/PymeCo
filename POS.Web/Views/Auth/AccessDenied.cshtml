@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow border-danger">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i class="bi bi-shield-exclamation text-danger" style="font-size: 5rem;"></i>
                    </div>

                    <h1 class="display-4 text-danger mb-3">403</h1>
                    <h2 class="mb-3">Acceso Denegado</h2>

                    <p class="text-muted mb-4">
                        No tienes permisos suficientes para acceder a este recurso.<br />
                        Si crees que esto es un error, contacta al administrador del sistema.
                    </p>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                            <i class="bi bi-house"></i> Ir al Inicio
                        </a>
                        <button onclick="history.back()" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Volver
                        </button>
                    </div>

                    <hr class="my-4" />

                    <div class="alert alert-info" role="alert">
                        <small>
                            <strong>💡 Información sobre roles:</strong><br />
                            <strong>Admin:</strong> Acceso total al sistema<br />
                            <strong>Vendedor:</strong> Gestión de clientes y visualización de productos<br />
                            <strong>Cajero:</strong> Solo visualización de datos
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>